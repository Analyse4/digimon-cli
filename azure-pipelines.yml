trigger:
  - master
variables:
  GOPATH: '/usr/gopath'
  GOBIN:  '$(GOPATH)/bin' # Go binaries path
  modulePath: '$(GOPATH)/src/github.com/$(build.repository.name)' # Path to the module's code

stages:

  - stage: Build
    jobs:

      - job: Build
        pool:
          vmImage: windows-latest
        steps:
          - bash: |
              mkdir -p $(GOBIN)
              mkdir -p $(GOPATH)/pkg
              mkdir -p $(modulePath)
              shopt -s extglob
              shopt -s dotglob
              mv * $(modulePath)
              echo '##vso[task.prependpath]$(GOBIN)'
              echo '##vso[task.prependpath]$(GOROOT)/bin'
            displayName: 'Set up the Go workspace'
          - bash: |
              cd $(modulePath)
              go version
              go get -v -t -d ./...
              if [ -f Gopkg.toml ]; then
                  curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh
                  dep ensure
              fi
              go install -v
            displayName: 'Get dependencies, then build'
#          - script: |
#              set %system.defaultWorkingDirectory%
#              move %GOBIN%/digimon-cli %system.defaultWorkingDirectory%
          - publish: 'C:'
            artifact: LinuxBinary

#          - bash: |
#              go version
#              go get -v -t -d ./...
#              if [ -f Gopkg.toml ]; then
#                  curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh
#                  dep ensure
#              fi
#              go install -v
#            workingDirectory: '$(modulePath)'
#          - bash: |
#              echo $(GOPATH)
#              echo $(GOBIN)
#              ls $(GOBIN)
#            displayName: 'testenv'
#            workingDirectory: '$(modulePath)'
#          - publish: $(GOBIN)/*
#            artifact: WindowsBinary